<?xml version="1.0" encoding="utf-8"?>
<!-- Locales offered in the system per-app language picker (Android 13+).
     English is the default resource set (values/), German lives in values-de/. -->
<locale-config xmlns:android="http://schemas.android.com/apk/res/android">
    <locale android:name="de" />
    <locale android:name="en" />
</locale-config>
